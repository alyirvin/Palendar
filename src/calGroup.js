import React, { useContext } from 'react';
import { UserContext } from './context';

class calGroup extends Component {
    constructor(props) {
        super(props);
        this.state = {
            group: [],
            users: []
        };
    }
}

render () {
    
}

export default calGroup;